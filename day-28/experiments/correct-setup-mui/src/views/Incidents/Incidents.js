import { Container, Typography } from "@mui/material";

export default function Incidents() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Incidents</Typography>
      <Typography>Incidents Content Area</Typography>
    </Container>
  );
}
