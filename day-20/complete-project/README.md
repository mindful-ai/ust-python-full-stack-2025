Build the model:
python train-save.py

Install FastAPI and Run the code: using the following commands:

pip install "fastapi[standard]"
uvicorn app:app --reload

Launch the streamlit application:
streamlit run streamlit_app.py